line1
% IMPORT-DATA test test.data
line2
% PLOT SELECT LOG(testsize) / LOG(2) AS x, bandwidth AS y FROM test WHERE funcname='ScanWrite64PtrUnrollLoop' ORDER BY x
line3
%% PLOT SELECT LOG(testsize) / LOG(2) AS x, bandwidth AS y FROM test
%% WHERE funcname='ScanRead64PtrUnrollLoop' ORDER BY x
\addplot[red] coordinates { (24,0.144) };
line4
{
indentation start
    %% PLOT SELECT testsize AS x, rate * 1e12 AS y FROM test
    %% WHERE funcname='ScanWrite64PtrUnrollLoop' ORDER BY x
    text
    %% PLOT SELECT testsize AS x, rate * 1e12 AS y FROM test
    %% WHERE funcname='ScanRead64PtrUnrollLoop' ORDER BY x
    \addplot[red] coordinates { (24,0.144) };
indentation end
}
line5